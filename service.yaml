apiVersion: v1
kind: Service
metadata:
  name: f5-mcp-server
  namespace: kagent
  labels:
    app: f5-mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
  selector:
    app: f5-mcp-server
